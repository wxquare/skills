# Test Prompts

用于验证 `excalidraw-diagram` 是否会被正确触发的典型请求。

1. `帮我画一张订单系统架构图，用 Excalidraw。`
2. `该画什么图来解释这条支付调用链？`
3. `Visualize this incident flow as a sequence diagram.`
4. `我需要一张 onboarding 图，让新人理解服务部署和主链路。`
5. `Draw an ERD for these tables and highlight the happy path.`
6. `怎么画这个 DDD bounded context relationship，更适合老板和研发一起看？`

